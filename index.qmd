---
title: "ORExt AAAS"
format:
  html:
    toc: false
    theme: cosmo
    page-layout: full
css: styles.css
---

# Welcome to the Oregon Essentialized Assessment Framework (EAF)

This site provides access to the **Alternate Academic Achievement Standards (AAAS)** for multiple content areas.\
Select a subject below to explore the standards.

```{=html}
<div class="card-grid">

  <!-- CTA Card -->
  <div class="card cta-card">
    <button onclick="printAllEAFs()" aria-label="Print all content areas to PDF" class="cta-button">
      <img src="images/print-icon.svg" alt="" class="cta-icon" />
      <span>Print All EAFs to PDF</span>
    </button>
    <p class="cta-text">Download Science, Mathematics, and ELA standards in a single PDF for easy sharing and printing.</p>
  </div>

  <!-- Science Card -->
  <div class="card">
    <img src="images/science-icon.svg" alt="Microscope icon representing Science" class="card-icon" />
    <h2><a href="science.html">Science</a></h2>
    <p>Explore Essentialized Science Standards, aligned with Oregon academic content standards.</p>
  </div>

  <!-- Math Card -->
  <div class="card">
    <img src="images/math-icon.svg" alt="Calculator icon representing Math" class="card-icon" />
    <h2><a href="math.html">Mathematics</a></h2>
    <p>View Essentialized Math Standards, organized by grade level and complexity.</p>
  </div>

  <!-- ELA Card -->
  <div class="card">
    <img src="images/ela-icon.svg" alt="Book icon representing English Language Arts" class="card-icon" />
    <h2><a href="ela.html">English Language Arts (ELA)</a></h2>
    <p>Access Essentialized ELA Standards with Low, Medium, and High instructional parameters.</p>
  </div>

</div>
```

```{=html}
<script>
async function printAllEAFs() {
  const pages = ["science.html", "math.html", "ela.html"];
  let content = "";
  for (const page of pages) {
    try {
      const res = await fetch(page);
      if (!res.ok) throw new Error(`HTTP ${res.status} - ${page}`);
      const html = await res.text();
      const main = html.match(/<main[^>]*>([\s\S]*?)<\/main>/i);
      if (main && main[1]) {
        content += `<h1>${page.replace(".html","").toUpperCase()}</h1>` + main[1] + "<hr/>";
      }
    } catch (e) {
      console.error(`Fetch error for ${page}:`, e);
    }
  }
  const w = window.open("", "_blank");
  w.document.write(`<html><head><title>All EAFs</title><style>
    body{font-family:Arial;margin:20px} h1{color:#004c97} hr{margin:2rem 0}
  </style></head><body>${content}</body></html>`);
  w.document.close();
  w.print();
}
</script>
```
